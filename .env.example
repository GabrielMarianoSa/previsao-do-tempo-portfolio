# Copie este arquivo para .env e preencha os valores

# Hugging Face (usado pelo backend em api/chat.js)
HF_API_KEY=

# (Opcional) Mantido apenas para referência; não é usado no backend atual
GEMINI_API_KEY=
